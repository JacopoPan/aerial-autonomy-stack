name: shell and python linting

on:
  schedule:
    - cron: '0 2 * * 6'
  workflow_dispatch: {}
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y shellcheck
      - name: Run shellcheck (warnings and errors)
        run: |
          find tools_and_docs simulation aircraft ground -name '*.sh' -print0 \
            | xargs -0 -r shellcheck -x -S warning

  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install ruff
      - name: Run ruff (syntax and undefined names)
        run: ruff check --select E9,F,B .
